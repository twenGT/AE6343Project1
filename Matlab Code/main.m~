clc; clear all; close all

designParametersF86L;

i = 1;
guess(i,:) = [20000 75];
delta = 1;

while deltaWF > 0.1

[WF_req, beta, TSL2WTO, WTO2SLD] = F86L(guess(i,1), guess(i,2));

WTO2SOPT = designPt(TSL2WTO, WTO2SLD);

[WF_av, WE] = weights(guess(i,1));

deltaWF = WF_av - WF_req;

i = i + 1;

guess(i,1) = guess(i-1,1) - deltaWF;
guess(i,2) = 

end